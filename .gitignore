node_modules
.env
*.log
# Runtime data — never commit (uploaded PDFs, study materials, WhatsApp sessions)
uploads/
examPdfs/
materials/
assignments/
videos/
.wwebjs_auth/
sbom.cdx.json

# Live whiteboard write-ahead journal (runtime, per-instance)
live-journal/
